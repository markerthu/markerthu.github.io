name: Probe tz
on: {workflow_dispatch: {}}
jobs:
  p:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - env: {GOATCOUNTER_TOKEN: "${{ secrets.GOATCOUNTER_TOKEN }}"}
        run: python3 scripts/probe_tz.py
